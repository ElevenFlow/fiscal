// Stub inicial — substituído na Task 2 pelo header completo (empresa switcher + busca + sino + user menu).
export function Header() {
  return (
    <header className="sticky top-0 z-30 flex h-16 items-center border-b bg-background px-4">
      <span className="text-sm text-muted-foreground">Header (a ser implementado na Task 2)</span>
    </header>
  );
}
